#include<stdio.h>
int main()
{
#ifndef ONLINE_JUDGE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
#endif
	int t, n, k1, k2, w, b;
	scanf("%d", &t);
	while (t--)
	{
		scanf("%d %d %d", &n, &k1, &k2);
		scanf("%d %d", &w, &b);

	}
	return 0;

}